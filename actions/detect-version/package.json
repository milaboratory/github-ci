{
  "name": "@milaboratories/actions.detect-version",
  "version": "1.0.1",
  "private": true,
  "description": "MiLaboratory code version detector",
  "main": "./lib/index.js",
  "scripts": {
    "build": "rm -rf lib dist && tsc && ncc build dist/main.js --minify --out lib --source-map",
    "format": "prettier --write '**/*.ts'",
    "format-check": "prettier --check '**/*.ts'",
    "lint": "eslint src/**/*.ts",
    "test": "vitest",
    "release": "npm run build && npm run format && npm run lint",
    "all": "npm run release && npm test"
  },
  "dependencies": {
    "@actions/core": "catalog:",
    "@actions/exec": "catalog:",
    "@actions/github": "catalog:",
    "@types/semver": "catalog:",
    "semver": "catalog:",
    "@milaboratories/github-ci-actions-milib": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "@vercel/ncc": "catalog:",
    "@milaboratories/platforma-build-configs": "workspace:*"
  }
}
