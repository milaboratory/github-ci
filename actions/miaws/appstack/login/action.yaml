name: Login to appstack Docker
author: 'MiLaboratories'
description: |
  Login to AppStack Docker registry

runs:
  using: "composite"

  steps:
    - name: Login
      uses: milaboratory/github-ci/actions/miaws@v2
      with:
        args: |-
          appstack
          login
